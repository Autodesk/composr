import cx from 'classnames';

class Feed extends React.Component {

    constructor(props) {
        super(props);
    }

    render() {
        const classes = cx({});

        return (
            <div className={ classes }>
                FEED
            </div>
        );
    }
}

export default Feed;

